# Issue #143: Loki Datasource Configuration
# Grafana가 시작될 때 자동으로 Loki 데이터소스를 등록합니다.

apiVersion: 1

datasources:
  - name: Loki
    type: loki
    access: proxy
    url: http://loki:3100
    isDefault: true
    editable: false
